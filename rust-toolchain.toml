# Keep local development, prek hooks, CI, and release checks on the crate's
# declared MSRV so a newer stable's lints and formatting never surprise CI.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
